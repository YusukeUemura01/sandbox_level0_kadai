import 'package:flutter/material.dart';


class FromFirebaseTodoPage extends StatelessWidget {
  const FromFirebaseTodoPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(),
    );
  }
}
